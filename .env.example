# Where PGlite stores the local Postgres data directory. Defaults to .pglite/
# Override only if you want to keep multiple databases side by side.
# PGLITE_DATA_DIR=.pglite

# Shared secret for Relay webhook signatures. The local relay:send script
# signs with this; the webhook handler verifies with it.
RELAY_WEBHOOK_SECRET=dev-secret
